# 立项：iirose 点歌插件 × QQ 音乐源（v0.6.0）

立项日期：2026-09-24
前置：`docs/可行性报告.md`（可行性验证已通过）· 用户决策已拍板

---

## 一、目标

在现有 iirose 点歌插件（v0.5.0，纯前端单文件，仅网易云）里新增 **QQ 音乐源**：
关键词或 QQ 音乐链接点播 → 能拿到完整直链的 QQ 歌曲直接播（卡片带 QQ 标识）；拿不到完整版（会员歌/无音质）**自动回退网易云同名单曲**。

## 二、范围

**做**
- QQ 音乐：关键词搜索、链接解析、歌词、封面、时长、直链
- 完整性判断（`quality` + `size` 双校验）
- 网易云回退链（QQ 不完整 → 网易云同名 → 明确失败提示）
- 卡片/事件的 `@2` 类型码 + 源标识 UI
- 直链提供方可插拔（先落地 vkeys，接口留好）

**不做**
- 会员歌完整播放（无绿钻，后端方案已出局）
- 会员歌 60 秒试听降级（用户选择：宁可回退也不给试听）
- 后端/服务端任何部署

## 三、技术方案要点

| 环节 | 做法 |
|---|---|
| 搜索 | `api.vkeys.cn/v2/music/tencent?word=&page=1&num=10` |
| 详情 | `?mid=<mid>`（返回 cover / interval / pay / quality） |
| 歌词 | `/v2/music/tencent/lyric?id=<id>` |
| 直链 | `/v2/music/tencent/geturl?mid=&quality=8`（128k 起步，够用） |
| 完整性判断 | **实际实现**：`quality` 含「试听」→ 不完整；否则看 geturl 返回的**实际文件码率** `kbps`（实测试听恒 28kbps、完整 ≥128kbps，与时长无关，短曲也不漏）；拿不到码率时退回 `size÷duration` 比值；两者都拿不到 → 判不完整（不敢确认就不播） |
| 直链修正 | **`http://` → `https://`**（iirose 是 https 页面，否则混合内容被拦）；大小写不敏感 |
| 回退 | QQ 不完整 → 网易云搜同名（优先歌手一致）→ 完整则播，否则提示"两平台都没有可播版本" |
| 点播 | 卡片 `m__4@2>歌名>歌手>封面>颜色>码率`；事件 `&1{... "b":"@2" ...}` |
| 链接识别 | `songDetail/<mid>`、`?songmid=`（i.y.qq.com/v8/playsong.html 官方移动端分享）、`?mid=`、`/n/yqq/song/<mid>.html`、纯 14 位 mid |

## 四、进度条与验收标准

| # | 里程碑 | 预计 | 验收标准（可执行） |
|---|---|---|---|
| M1 | 数据层：QQ 搜索/详情/歌词/直链 + 完整性判断 | 30 min | 脚本对 3 类样本（免费完整/会员试听/无音质）判定全部正确 |
| M2 | 点播链路打通：`@2` 卡片 + 事件 | 30 min | 浏览器里点播 QQ 免费歌，iirose 实际播放出声 |
| M3 | 网易云回退链 + 失败提示 | 30 min | 会员歌自动回退网易云有版权的同名曲；无版权时给出明确文案 |
| M4 | 输入识别：QQ 链接 / 关键词 分流 | 20 min | 粘贴 `y.qq.com` 链接能点播；关键词优先 QQ、无结果自动转网易云 |
| M5 | UI：源标识（QQ/网易云）+ 卡片配色复用 | 20 min | 卡片上能看出这首来自哪个平台，封面主色逻辑不回归 |
| M6 | 端到端验收测试（浏览器实测） | 30 min | 4 类场景各点一次：免费歌/会员歌回退/QQ链接/纯关键词，全部有正确结果 |
| M7 | 交付沉淀：v0.6.0 提交 → 推送 → jsdelivr 生效 | 15 min | 朋友注入地址拉到的脚本含 QQ 源；README/技能库更新 |

合计约 3 小时。**每个里程碑做完会停下来报结果**，不一次性糊完。

### 执行状态（2026-09-24 当日）

| # | 状态 | 说明 |
|---|---|---|
| M1 | ✅ 完成 | 数据层 + 独立子 agent 审查（3 项阻塞 + 8 项边界问题）全部修复；验收脚本 **53 PASS / 0 FAIL** |
| M2 | ✅ 完成 | 协议层类型码参数化；点播按 `source` 分流；浏览器实测发出正确的 `@2` 消息 |
| M3 | ✅ 完成 | 回退链实测通过：会员歌 → 网易云同名单曲，提示含回退到的歌手名 |
| M4 | ✅ 完成 | 粘贴 `i.y.qq.com/v8/playsong.html?songmid=` 实测解析并点播成功 |
| M5 | ✅ 完成 | 源徽标（红=网易云 / 绿=QQ）+ 时长；QQ 卡片色兜底腾讯绿（封面域无 CORS） |
| M6 | 🔶 自动化部分完成 | 4 场景 mock iirose 实测全绿；待北海在真实 iirose 房间点一次 |
| M7 | ⏸ 等确认 | 推送 GitHub / jsdelivr 生效属对外发布，等北海点头 |

### 实施中与立项不同的地方（实测纠正）

1. **完整性判断换判据**：立项原计划「`size` 与官方 `size_128mp3` 比对」，但 vkeys 搜索不返回官方 size。实际改用 geturl 的 `kbps`（实测试听恒 28kbps、完整 319kbps）——比比值法更稳，短曲也不漏判。
2. **链接识别范围扩大**：官方移动端分享（`i.y.qq.com/v8/playsong.html?songmid=`）是最常见形式，立项时漏了；这也正是 vkeys 返回的 `link` 字段格式（用户复制回来粘贴，原实现解析不出）。
3. **`pay` 字段不可用**：实测标「付费」的歌（莫扎特 k448）也能完整播放，不能拿来预筛会员歌。
4. **QQ 封面域无 CORS**：`y.qq.com` 不带 `Access-Control-Allow-Origin`，canvas 取色必然失败 → 兜底色腾讯绿 `31c27c`。
5. **回退可能落到翻唱版**：网易云没有周杰伦版权，回退同名会命中翻唱（实测「晴天(深情版) - Lucky小爱」）→ 补了「优先同歌手」匹配，并在提示里如实报出回退到谁的版本。

### 回归测试产物

- `tests/m1_verify.js` —— M1 数据层验收（53 项：提取插件真实代码在 Node 里跑，含审查问题的回归用例）。用法 `node tests/m1_verify.js`
- `tests/mock-iirose.html` —— 假 socket 的 iirose 房间，端到端驱动真实插件 UI（需起静态服）
- `docs/审查报告-M1.md` —— 独立子 agent 的审查报告（存档）

## 五、风险与对策

| 风险 | 对策 |
|---|---|
| vkeys 挂了 → QQ 源整体不可用 | 直链提供方做成列表 + 逐个降级；失败时透明回退网易云 |
| vkeys 加 key / 限流 | 监控报错文案；预留可替换实现 |
| 回退到网易云也失败（周杰伦等两平台都无版权） | UI 明确提示，不静默失败 |
| 完整性判断误判（把完整当试听） | 双校验 + 用官方 `size` 兜底，M1 单独验收 |

## 六、交付物

- 更新的 `iirose-dianbo.js`（v0.6.0）+ README
- 可行性报告与本文档（归档在 `D:\iirose-js\research\`）
- 技能库 `iirose-platform` 的 `references/music-api.md` 增补 QQ 源章节（含踩坑：https 化、完整性判断、vkeys 接口）
